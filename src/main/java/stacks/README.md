# Stack tasks

Implemented stack problems:

- `MinStack.java` - stack supporting `getMin()` in constant time
- `StacksUsingArrays.java` - stack implementation backed by arrays

Run class `main` methods to see sample usage.

