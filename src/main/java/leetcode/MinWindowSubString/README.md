# LeetCode 76 - Minimum Window Substring

## Problem
Given strings `s` and `t`, return the smallest window in `s` that contains every character in `t`.

## Implementation
- Source: `Solution.java`
- Package: `leetcode/MinWindowSubString`
- Technique: sliding window with frequency counting arrays

## Note
This implementation currently assumes lowercase alphabet characters (`a-z`).

